---
phase: 03-pwa-frontend
plan: 05
type: execute
wave: 3
depends_on: [03-04]
files_modified:
  - web/README.md
autonomous: false
requirements: [PWA-01, PWA-02, PWA-03, OFFR-01, OFFR-02, OFFR-03, OFFR-04, OFFR-05, OFFR-06, HIST-01, HIST-02, HIST-03]

must_haves:
  truths:
    - "The built PWA, served over a secure context (localhost), is installable to a home screen with the ColaApp name and the trademark-safe icon"
    - "Once installed/loaded, going offline still shows the last-fetched data (offline last-data)"
    - "Editing a data file and reloading online shows the new data on a subsequent load (fresh-when-online, not stale forever)"
    - "Each of the six fixture states (offer/no_offer/upcoming/error/stale/unavailable) renders the correct hero, cards, badges, stale markers, and graph; a needsReview entry never headlines the hero/best-deal, but DOES surface as a flagged amber 'bitte prüfen' review card (post-2026-06 merge: commit 81bb27a)"
  artifacts:
    - path: "web/README.md"
      provides: "How to dev/build/preview the PWA, the ?state= fixture switch, and the localhost install/offline test procedure"
      contains: "?state="
  key_links:
    - from: "installed PWA"
      to: "data/*.json(l)"
      via: "Workbox NetworkFirst, 3s timeout (fresh-when-online + offline last-data fallback) — changed from StaleWhileRevalidate, commit 94892ef"
      pattern: "NetworkFirst"
---

<objective>
Verify, with a human in the loop, that the PWA actually installs, works offline with last-fetched
data, refreshes when back online, and renders every one of the six UI-state fixtures correctly —
the success-criteria bar that only manual install/offline testing can confirm. Document the
dev/build/preview + fixture-switch + install/offline procedure in web/README.md so the maintainer
(and Phase 4) can reproduce it.

Purpose: Close PWA-01/02/03 (installability, offline-last-data, fresh-when-online) and visually
confirm OFFR-01..06 + HIST-01..03 across all six states after Claude has automated the build. This
plan automates everything possible (build, serve, README) and pauses only for the genuinely
human-only checks (install prompt, offline toggle, visual state review on a phone/Chromium).
Output: web/README.md + a recorded human verification of install/offline/fresh + the six states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pwa-frontend/03-UI-SPEC.md
@.planning/phases/03-pwa-frontend/03-RESEARCH.md
@.planning/phases/03-pwa-frontend/03-01-SUMMARY.md
@.planning/phases/03-pwa-frontend/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, serve, and author the dev/install/offline README</name>
  <files>web/README.md</files>
  <read_first>
    - .planning/phases/03-pwa-frontend/03-RESEARCH.md ("Environment Availability": localhost is a valid secure context for SW; Pitfall 4 subpath is a Phase-4 concern)
    - .planning/phases/03-pwa-frontend/03-04-SUMMARY.md (the ?state= switch and fixture names)
    - .planning/phases/03-pwa-frontend/03-UI-SPEC.md (the six states + expected visuals to spot-check)
    - web/package.json (dev/build/preview/test scripts)
  </read_first>
  <action>
    Run `cd web && npm run build` to confirm a clean production build, then `npm run preview` (Vite
    preview serves dist over http://localhost — a valid secure context for service workers, so
    install/offline can be tested now without the Phase-4 GitHub Pages HTTPS). Author web/README.md
    documenting: the stack and pinned versions; `npm run dev` / `npm run build` / `npm run preview`
    / `npm test`; the `?state=offer|no_offer|upcoming|error|stale|unavailable` dev fixture switch and
    what each state should show; and a step-by-step localhost install + offline test procedure
    (open in Chrome/Android Chrome over localhost, install to home screen, toggle offline / DevTools
    Application > Service Workers Offline, reload to confirm last-data, edit a public/data file and
    reload online to confirm fresh-when-online). Note that the production HTTPS install at the
    GitHub Pages subpath is finalized in Phase 4 (set Vite `base`) and is out of scope here. Leave
    the preview server running (or print the exact command) for the human-verify task that follows.
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - web/README.md exists and documents the ?state= switch (all six state names), the four npm scripts, and the localhost install + offline + fresh-when-online test steps
    - README notes that the Pages-subpath HTTPS install is a Phase-4 concern (Vite `base`)
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && node -e "const r=require('fs').readFileSync('README.md','utf8');for(const t of ['?state=','offer','no_offer','upcoming','error','stale','unavailable','npm run preview','offline','base'])if(!r.includes(t))throw new Error('README missing '+t);console.log('OK')"</automated>
  </verify>
  <done>A clean build is produced, preview is served on localhost, and README documents dev/build/preview, the six-state switch, and the install/offline/fresh test procedure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — install, offline, fresh-when-online, and the six states</name>
  <files>(none — verification checkpoint)</files>
  <action>
    Checkpoint only — Claude writes no code here. Before pausing, Claude has already produced a clean
    production build, served it on http://localhost via `npm run preview`, and authored the README.
    This task pauses execution for the human to perform the install / offline / fresh-when-online and
    six-state visual checks documented under how-to-verify, then resume on approval.
  </action>
  <what-built>
    A built, installable PWA served on http://localhost via `npm run preview`, with a `?state=`
    switch to render each of the six UI-state fixtures. Claude has produced the build, the Workbox
    service worker (precache + NetworkFirst /data/ route, post-merge), the manifest (standalone,
    192/512/maskable, theme #1A1D21), and the README test procedure.
  </what-built>
  <how-to-verify>
    Open Chromium (desktop Chrome or Android Chrome) at the preview URL printed by `npm run preview`
    (e.g. http://localhost:4173). Then:
    1. INSTALL (PWA-01): Confirm the install prompt/affordance appears; install to the home
       screen/app list. Confirm the app name is "ColaApp" and the icon is the trademark-safe bottle
       glyph on a dark tile (NOT Coca-Cola red/logo), with the maskable variant not clipped.
    2. OFFLINE LAST-DATA (PWA-02): With the app loaded, go offline (DevTools Application > Service
       Workers > Offline, or device airplane mode) and reload. Confirm the last-fetched data still
       renders (hero/cards/graph/footer all present, not a blank/dino page).
    3. FRESH-WHEN-ONLINE (PWA-03): Go back online. Edit web/public/data/current-offers.json (e.g.
       change REWE price), rebuild/refresh the data, and reload. Confirm the new value appears on the
       FIRST online reload (NetworkFirst fetches fresh — not stale forever; the old SWR one-open lag
       that needed a second reload was the bug commit 94892ef fixed).
    4. SIX STATES: Visit ?state=offer, ?state=no_offer, ?state=upcoming, ?state=error,
       ?state=stale, ?state=unavailable in turn. Confirm:
       - offer: hero shows REWE €9,99 "gültig bis So 21.06. · 0,83 €/l"; Edeka (needsReview) does
         NOT headline the hero, but DOES show its own amber "bitte prüfen" review card (muted price,
         ranked below upcoming, above no_offer); Lidl "kein Angebot" (grey), Kaufland "Fehler" (amber), Wasgau
         "nicht automatisch verfügbar" (blue i) are three distinct chips.
       - upcoming: hero empty "Kein aktuelles Angebot" + nudge "Nächstes Angebot ab So 21.06. …";
         REWE card shows a "demnächst — ab …" badge above no_offer cards.
       - stale: REWE card shows a "⚠ veraltet" chip (per-store lastUpdated 2026-06-05 > 3 days);
         footer shows the file-level timestamp; no global stale banner.
       - error/no_offer/unavailable: the correct distinct chips and ordering (active → upcoming →
         no_offer → unavailable/error).
       - graph: per-store colored lines (REWE/Edeka/Lidl/Kaufland), Wasgau greyed in the legend with
         "nicht automatisch verfügbar"; sparse stores show markers only with broken (never
         interpolated) lines; if 0 history points, "Noch keine Daten" panel shows instead of an
         empty axis.
    5. Confirm German throughout, de-DE number/date formatting (€9,99 / 0,83 €/l / 21.06.2026),
       and the neutral (non-Coca-Cola-branded) utility look.
  </how-to-verify>
  <verify>
    <human-check>Human confirms install (PWA-01), offline last-data (PWA-02), fresh-when-online (PWA-03), and all six fixture states render per how-to-verify.</human-check>
  </verify>
  <done>Human types "approved" after install + offline + fresh + all six states verify correctly; any defect is described and fixed before approval.</done>
  <resume-signal>Type "approved" if install + offline + fresh + all six states render correctly, or describe what's wrong (which state, which element) so it can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed PWA ↔ cache/network | The service worker decides offline vs fresh behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering/DoS | Stale-forever after install (PWA-03 regression) | mitigate | Human step 3 explicitly verifies fresh-when-online (NetworkFirst 3s + autoUpdate; the SWR one-open lag was itself this regression, fixed in 94892ef); a stale-forever result fails the checkpoint |
| T-03-11 | Spoofing (brand) | Icon reproduces the Coca-Cola trademark | mitigate | Human step 1 confirms the icon is the generic trademark-safe glyph, not the Coca-Cola logo/red |

No new code surface beyond README; this plan is verification. Other STRIDE categories N/A (read-only static PWA).
</threat_model>

<verification>
- `cd web && npm run build` clean; preview served on localhost.
- Human confirms: installable (PWA-01), offline last-data (PWA-02), fresh-when-online (PWA-03), and
  all six fixture states render correctly with honest three-state cards, badges, stale markers, and
  the gap-honest graph (OFFR-01..06, HIST-01..03).
</verification>

<success_criteria>
- All five ROADMAP Phase-3 success criteria are confirmed end-to-end against the mocks: hero
  best-deal/empty, three distinct store states, upcoming + history graph with honest sparse/gap
  handling, last-updated + per-store stale markers, and install + offline + fresh-when-online.
</success_criteria>

<output>
Create `.planning/phases/03-pwa-frontend/03-05-SUMMARY.md` when done, recording the human
verification outcome for each of PWA-01/02/03 and the six states.
</output>
