---
phase: 03-pwa-frontend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/chart/history.js
  - web/test/chart.test.mjs
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03]

must_haves:
  truths:
    - "Chart data-prep builds one shared sorted date x-axis and one y-series per line-store (REWE, Edeka, Lidl, Kaufland) with null for dates a store has no observation; Wasgau is never a series"
    - "A no-offer gap between two observations is a null in the series so the line breaks and is never interpolated across"
    - "A store with fewer than 3 points renders markers only (no connecting line); 3+ points connect"
    - "Zero history points renders the 'Noch keine Daten' empty state instead of an empty uPlot axis"
    - "The lowest line at any date is visually the best price across stores (HIST-01)"
  artifacts:
    - path: "web/src/chart/history.js"
      provides: "Pure prepareChartData(historyLines) + parseHistoryJsonl(text) + uPlot renderHistory(container, historyLines) with spanGaps:false, paths:()=>null for <3 points, per-store palette, cold-start panel, ResizeObserver"
      exports: ["prepareChartData", "parseHistoryJsonl", "renderHistory"]
    - path: "web/test/chart.test.mjs"
      provides: "node --test suite for data-prep: shared axis, null gaps, Wasgau excluded, point counts, JSONL parse"
      contains: "node:test"
  key_links:
    - from: "web/src/chart/history.js"
      to: "uplot package + uplot CSS"
      via: "import uPlot from 'uplot'"
      pattern: "uplot"
    - from: "web/src/chart/history.js"
      to: "no-offer gaps"
      via: "null y-values + spanGaps:false (never interpolate)"
      pattern: "spanGaps"
---

<objective>
Build the price-history chart: a pure, testable data-prep half (align history lines to a shared
date axis with null gaps, count points per store, exclude Wasgau) and the uPlot rendering half that
honestly encodes the sparse/gap rules (spanGaps:false, markers-only for <3 points, per-store
palette + legend, cold-start empty panel). Independent of the derive/format/render files, so it runs
in parallel in Wave 1.

Purpose: Satisfy HIST-01 (best price over time = lowest line), HIST-02 (no misleading trend, no
interpolation across no-offer gaps), and HIST-03 (per-store lines; Wasgau none) — with the
load-bearing gap logic (RESEARCH landmine 3) proven by unit tests on the data-prep half.
Output: web/src/chart/history.js + passing web/test/chart.test.mjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pwa-frontend/03-RESEARCH.md
@.planning/phases/03-pwa-frontend/03-PATTERNS.md
@.planning/phases/03-pwa-frontend/03-UI-SPEC.md
@contract/types.d.ts
@scraper/select.mjs
@test/scraper.select.test.mjs
@data/price-history.jsonl
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure chart data-prep (shared axis, null gaps, point counts, JSONL parse)</name>
  <files>web/src/chart/history.js (data-prep exports), web/test/chart.test.mjs</files>
  <read_first>
    - .planning/phases/03-pwa-frontend/03-RESEARCH.md (Pattern 4 prepareChartData; Pitfall 3 interpolation; Pitfall 8 cold-start)
    - .planning/phases/03-pwa-frontend/03-PATTERNS.md ("web/src/chart/history.js" section — data-prep analog is scraper/select.mjs; JSONL line model)
    - contract/types.d.ts (HistoryLine shape: {date,store,price,pricePerLitre,validFrom,validTo}; price integer cents)
    - data/price-history.jsonl (real 3-line fixture: REWE 2026-06-01, Kaufland 2026-06-08, REWE 2026-06-15)
    - test/scraper.select.test.mjs (test harness style)
  </read_first>
  <behavior>
    - parseHistoryJsonl(text): split on newlines, drop empty/whitespace lines, JSON.parse each -> HistoryLine[]. Tolerates a trailing newline.
    - prepareChartData(historyLines): STORES_WITH_LINES=['REWE','Edeka','Lidl','Kaufland'] (Wasgau excluded). x = unique sorted dates -> epoch seconds (Date.parse(d+'T00:00:00Z')/1000). For each line-store, a y-array (price/100) aligned to x with null where the store has no observation on that date. Returns {data:[x,...series], dates, stores:STORES_WITH_LINES, pointCounts:{store:nonNullCount}}.
    - On data/price-history.jsonl: dates ['2026-06-01','2026-06-08','2026-06-15']; REWE series [10.99,null,9.99] (pointCount 2 -> markers only); Kaufland [null,10.49,null] (pointCount 1); Edeka & Lidl all-null (pointCount 0). Wasgau absent from series entirely.
    - Empty input -> {data:[[]], dates:[], pointCounts all 0} so renderHistory can detect cold-start.
  </behavior>
  <action>
    Create web/src/chart/history.js and export the pure functions parseHistoryJsonl(text) and
    prepareChartData(historyLines) per RESEARCH Pattern 4. Define STORES_WITH_LINES =
    ['REWE','Edeka','Lidl','Kaufland'] (Wasgau intentionally absent — D-12/HIST-03). x-axis =
    unique sorted dates converted to epoch SECONDS (Date.parse(date+'T00:00:00Z')/1000). Each store
    series maps over the shared dates, emitting price/100 where that store has a HistoryLine on that
    date else null (null => gap, NOT interpolation — RESEARCH Pitfall 3). Also return a pointCounts
    map (non-null count per store) so the render half can choose markers-only vs line. Write
    web/test/chart.test.mjs (node:test / node:assert/strict, readJson/readFile relative to
    import.meta.url, pointing at the repo-root data/price-history.jsonl via ../../../data) asserting:
    parseHistoryJsonl handles the 3-line file + trailing newline, the shared sorted date axis, REWE
    series === [value, null, value] with a null gap at 2026-06-08, Kaufland pointCount 1, Wasgau
    never appears in the series list, and empty input yields the cold-start-detectable shape.
  </action>
  <acceptance_criteria>
    - `cd web && node --test test/chart.test.mjs` passes with 0 failures
    - prepareChartData(parseHistoryJsonl(<data/price-history.jsonl>)).stores has length 4 and does NOT include 'Wasgau' — asserted
    - REWE series equals [10.99, null, 9.99] (null at the 2026-06-08 index where REWE has no observation) — asserted, proving no interpolation
    - pointCounts for REWE is 2 and Kaufland is 1 — asserted (drives markers-only branch)
    - prepareChartData([]) returns a shape whose dates array is empty — asserted (cold-start signal)
  </acceptance_criteria>
  <verify>
    <automated>cd web && node --test test/chart.test.mjs</automated>
  </verify>
  <done>history.js data-prep exports build the aligned multi-store axis with null gaps and per-store point counts; tests prove Wasgau exclusion, the null gap, and the cold-start shape.</done>
</task>

<task type="auto">
  <name>Task 2: uPlot rendering (gap/markers rules, palette, legend, cold-start, resize)</name>
  <files>web/src/chart/history.js (renderHistory export)</files>
  <read_first>
    - .planning/phases/03-pwa-frontend/03-RESEARCH.md (Pattern 5 seriesFor + paths:()=>null; "Code Examples: uPlot construction"; Pitfall 8 cold-start)
    - .planning/phases/03-pwa-frontend/03-UI-SPEC.md ("Price-history graph" anatomy + per-store palette/marker table + legend + graph.empty copy)
    - .planning/phases/03-pwa-frontend/03-CONTEXT.md (D-11/D-12/D-13-graph/D-14)
    - web/src/format/format.js (formatDate/de-DE — for axis tick labels; from Plan 02, may not exist yet at start; if absent, inline the same Intl call and leave a TODO to import once Plan 02 lands — but prefer importing)
  </read_first>
  <action>
    Add renderHistory(container, historyLines) to web/src/chart/history.js. import uPlot from
    'uplot' and 'uplot/dist/uPlot.min.css'. Call prepareChartData; if dates.length===0, render the
    cold-start panel into container with the UI-SPEC copy graph.empty 'Noch keine Daten' +
    graph.emptyBody 'Sobald Preise gesammelt sind, erscheint hier der Verlauf.' (build DOM with
    textContent/createElement, never innerHTML) and return WITHOUT constructing uPlot (Pitfall 8).
    Otherwise build uPlot opts: width container.clientWidth, height ~240, scales.x.time true; x-axis
    values formatter using Intl de-DE {day:'2-digit',month:'2-digit'} on t*1000; y-axis values
    formatter rendering '<n>,<nn> €' (comma decimal). One series per STORES_WITH_LINES via a
    seriesFor(store,color,pointCount) helper (RESEARCH Pattern 5): stroke = the UI-SPEC palette
    color (REWE #C2143C, Edeka #1B5FB0, Lidl #0B7A3B, Kaufland #7A3FB0), width 2, spanGaps:false
    (null breaks the line, NEVER interpolate — D-13-graph), points {show:true,size:7}, and
    paths:()=>null when pointCount<3 (markers only) else undefined (default linear path). Construct
    new uPlot(opts, data, container). Render the legend below the chart listing all five stores with
    a color swatch + name; Wasgau is greyed/struck with the note graph.legendUnavailable
    'nicht automatisch verfügbar' (D-12). Attach a ResizeObserver that calls
    plot.setSize({width:container.clientWidth,height:240}) on resize (RESEARCH Code Examples).
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 (uPlot import + CSS resolve; chart module bundles)
    - history.js source contains spanGaps:false and a paths reference that returns null for the sparse (<3 point) branch (grep: `spanGaps` present and `paths` present with a `< 3` / `<3` count guard)
    - history.js references all four line colors #C2143C, #1B5FB0, #0B7A3B, #7A3FB0 and does NOT add a Wasgau series (no `'Wasgau'` pushed into the series array)
    - Cold-start branch returns before `new uPlot(` when dates.length===0, and renders 'Noch keine Daten' via textContent (grep: 'Noch keine Daten' present; no innerHTML assignment with data)
    - A ResizeObserver calling setSize is wired (grep: 'ResizeObserver' and 'setSize' present)
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && node -e "const s=require('fs').readFileSync('src/chart/history.js','utf8');for(const t of ['spanGaps','paths','#C2143C','#1B5FB0','#0B7A3B','#7A3FB0','Noch keine Daten','ResizeObserver','setSize'])if(!s.includes(t))throw new Error('history.js missing '+t);if(/\.innerHTML\s*=/.test(s))throw new Error('innerHTML forbidden in render');console.log('OK')"</automated>
  </verify>
  <done>renderHistory draws per-store lines with honest gap/markers rules, the Wasgau-aware legend, and a cold-start panel; build succeeds and the load-bearing uPlot options are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history JSONL → chart DOM/canvas | Fetched data rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering (correctness) | Interpolated line across no-offer gaps implies prices that never existed | mitigate | spanGaps:false + null gaps per series; data-prep test asserts the null gap (RESEARCH Pitfall 3) |
| T-03-06 | Tampering (DOM XSS) | Cold-start / legend text injected as HTML | mitigate | build all chart DOM (empty panel, legend) with textContent/createElement, never innerHTML (ASVS V5) |

uPlot renders to a canvas from numeric arrays; store names come from the frozen STORES allow-list. No auth/session/network/secret surface — other STRIDE categories N/A.
</threat_model>

<verification>
- `cd web && node --test test/chart.test.mjs` passes; data-prep proves the shared axis, null gaps, Wasgau exclusion, point counts, cold-start shape.
- `cd web && npm run build` succeeds with uPlot bundled.
- Source contains spanGaps:false, the <3-point markers-only branch, the four palette colors, the cold-start panel, and a ResizeObserver.
</verification>

<success_criteria>
- HIST-01 (lowest line = best price), HIST-02 (no interpolation, markers-only for sparse),
  HIST-03 (per-store lines, Wasgau none) are encoded and the data-prep is unit-tested.
</success_criteria>

<output>
Create `.planning/phases/03-pwa-frontend/03-03-SUMMARY.md` when done.
</output>
