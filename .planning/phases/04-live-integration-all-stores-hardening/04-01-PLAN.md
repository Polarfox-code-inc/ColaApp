---
phase: 04-live-integration-all-stores-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/heartbeat.mjs
  - test/heartbeat.test.mjs
  - web/vite.config.js
autonomous: true
requirements: [INFR-02, INFR-03]
must_haves:
  truths:
    - "Running `node scripts/heartbeat.mjs` writes data/heartbeat.json with a top-level lastRun ISO timestamp"
    - "The heartbeat never mutates any per-store lastUpdated value in current-offers.json or status.json"
    - "The Vite build emits assets under the /ColaApp/ subpath; start_url/scope/icon srcs stay relative"
  artifacts:
    - path: "scripts/heartbeat.mjs"
      provides: "Keepalive heartbeat writer (D-04/D-05) — separate file, no contract change"
      contains: "lastRun"
    - path: "test/heartbeat.test.mjs"
      provides: "Locks the D-05 invariant (per-store lastUpdated untouched)"
    - path: "web/vite.config.js"
      provides: "base: '/ColaApp/' for the GitHub Pages project subpath (D-08)"
      contains: "base"
  key_links:
    - from: "scripts/heartbeat.mjs"
      to: "data/heartbeat.json"
      via: "atomic same-dir temp+rename write (mirrors scraper/io.mjs writeAtomic)"
      pattern: "heartbeat\\.json"
    - from: "web/vite.config.js"
      to: "GitHub Pages /ColaApp/ subpath"
      via: "Vite base option (vite-plugin-pwa auto-prepends to sw.js/manifest)"
      pattern: "base:\\s*'/ColaApp/'"
---

<objective>
Land the two code-touching pieces of the live-loop wiring before the CI workflow consumes them: (1) a standalone keepalive heartbeat writer that bumps a "last successful run" timestamp without ever touching per-store freshness (D-04/D-05), and (2) the one-line Vite `base: '/ColaApp/'` change that makes the PWA shell + Workbox SW correct under the GitHub Pages project subpath (D-08).

Purpose: The workflow in Plan 02 calls `node scripts/heartbeat.mjs` after scraping and builds with the subpath set; both must exist and be proven before the pipeline is assembled. Keeping the heartbeat in a SEPARATE file (Option A) means the frozen `.strict()` contract is never edited (avoids Landmine L-3).
Output: `scripts/heartbeat.mjs`, `test/heartbeat.test.mjs`, edited `web/vite.config.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-integration-all-stores-hardening/04-CONTEXT.md
@.planning/phases/04-live-integration-all-stores-hardening/04-RESEARCH.md
@.planning/phases/04-live-integration-all-stores-hardening/04-PATTERNS.md
@scraper/io.mjs
@scraper/merge.mjs
@web/vite.config.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keepalive heartbeat writer (D-04/D-05, Option A — separate file)</name>
  <files>scripts/heartbeat.mjs, test/heartbeat.test.mjs</files>
  <read_first>
    - scraper/io.mjs — the writeAtomic same-dir temp+rename seam (lines 67-71), DEFAULT_DATA_DIR resolution via fileURLToPath/import.meta.url (lines 22-25), and the "No dependencies" constraint (line 15)
    - scraper/merge.mjs — confirm per-store lastUpdated lives at stores[].lastUpdated (lines 68, 87, 97), the values D-05 protects
    - .planning/phases/04-live-integration-all-stores-hardening/04-RESEARCH.md — Pattern 4 Option A (the heartbeat shape { lastRun: ISO }) and the D-05 invariant
    - .planning/phases/04-live-integration-all-stores-hardening/04-PATTERNS.md — the heartbeat Pattern Assignment section (atomic + 2-space + trailing-newline conventions to mirror)
  </read_first>
  <behavior>
    - Test 1: after running the script's exported write function, data/heartbeat.json exists and parses to an object with a top-level `lastRun` that is a valid ISO-8601 string (Date.parse non-NaN)
    - Test 2 (D-05 invariant): given a fixture status.json containing stores[] with known lastUpdated values, running the heartbeat does NOT read, modify, or rewrite status.json or current-offers.json — assert those files' stores[].lastUpdated bytes are byte-identical before and after
    - Test 3: re-running the heartbeat overwrites heartbeat.json with a newer lastRun and leaves a trailing newline + 2-space indentation (serialization convention)
  </behavior>
  <action>
    Create scripts/heartbeat.mjs as a zero-dependency ESM module (node:fs/promises, node:crypto, node:path, node:url only — mirror scraper/io.mjs's "No dependencies" rule; Phase 4 adds NO npm packages). Resolve the data dir relative to the file via `dirname(fileURLToPath(import.meta.url))` then `join(HERE, "..", "data")` — the script lives in scripts/ (one level under repo root) so the hop is "../data". Export a function (e.g. `writeHeartbeat(dataDir)`) AND run it when invoked as the entry module so `node scripts/heartbeat.mjs` works in CI. Write data/heartbeat.json atomically by mirroring io.mjs writeAtomic verbatim (same-directory `${target}.${randomBytes(6).hex}.tmp` then rename — EXDEV-safe) with content `JSON.stringify({ lastRun: new Date().toISOString() }, null, 2) + "\n"` (the repo's 2-space + trailing-newline convention). Per D-05 the script MUST NOT touch status.json/current-offers.json or any stores[].lastUpdated — a separate file satisfies this by construction; do not import merge.mjs or write into stores[]. Create test/heartbeat.test.mjs as a node:test suite (matches `npm test` = `node --test`) covering the three behaviors above, using a temp data dir for isolation. Do NOT place the heartbeat write inside scraper/index.mjs (keeps the scraper's atomic validate-before-write path untouched per RESEARCH Pattern 4).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes including the new test/heartbeat.test.mjs (existing 84+ suite stays green)
    - `node scripts/heartbeat.mjs` writes data/heartbeat.json with a valid ISO lastRun, 2-space indent, trailing newline
    - The D-05 invariant test proves status.json/current-offers.json stores[].lastUpdated are unchanged by the heartbeat
    - scripts/heartbeat.mjs imports only node: built-ins (no npm deps, no merge.mjs import)
  </acceptance_criteria>
  <done>scripts/heartbeat.mjs writes a separate, atomic, convention-conformant heartbeat file and a passing test locks the D-05 invariant; the frozen contract is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Set Vite base to '/ColaApp/' for the Pages subpath (D-08)</name>
  <files>web/vite.config.js</files>
  <read_first>
    - web/vite.config.js — the current defineConfig object; the pre-existing Phase-4 comment at lines 8-9; the relative start_url/scope (lines 24-25) and bare-relative icon srcs (lines 30-32); the function urlPattern SW route matching url.pathname (line 42)
    - .planning/phases/04-live-integration-all-stores-hardening/04-PATTERNS.md — the web/vite.config.js Pattern Assignment ("Do NOT touch" list) and the runtime-fetch confirmation
    - .planning/phases/04-live-integration-all-stores-hardening/04-RESEARCH.md — Pattern 6 (base auto-prepended by vite-plugin-pwa; icon issue #713; load.js relative-fetch subtlety)
  </read_first>
  <action>
    Add a single top-level `base: '/ColaApp/'` key to the defineConfig object in web/vite.config.js, placed before `server` (D-08; the Pages URL is https://polarfox-code-inc.github.io/ColaApp/). Change NOTHING else. Specifically do NOT alter: manifest start_url/scope (keep relative './' — absolute would break installability under the subpath, RESEARCH Anti-Pattern), the bare-relative icon srcs ('icon-192.png' etc. — plugin issue #713 means icon src is NOT base-prepended, so a leading slash would 404), or the function urlPattern SW route (it matches url.pathname so it is already subpath-safe, D-07). vite-plugin-pwa@1.3.0 auto-prepends base to the generated sw.js/registerSW.js/manifest.webmanifest registration, so setting base alone is sufficient. No change to web/src/data/load.js (its relative `./data/...` fetch resolves against document.baseURI = /ColaApp/, and index.html has no `<base href>` override — verified).
  </action>
  <verify>
    <automated>cd web && npm ci && npm run build && node -e "const fs=require('fs');const html=fs.readFileSync('dist/index.html','utf8');if(!/\/ColaApp\//.test(html)){console.error('base /ColaApp/ not applied to built asset URLs');process.exit(1)};console.log('base applied OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (in web/) succeeds
    - dist/index.html references its JS/CSS assets under /ColaApp/ (base applied)
    - dist/ contains sw.js + manifest.webmanifest with registration under the subpath; dist/icon-192.png, dist/icon-512.png, dist/icon-maskable-512.png exist and the manifest icon srcs remain bare-relative
    - manifest start_url and scope remain './' (not absolute)
  </acceptance_criteria>
  <done>web/vite.config.js sets base:'/ColaApp/' and a production build emits a subpath-correct shell with relative manifest paths and icons present at the expected names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local/CI filesystem → committed data/ | The heartbeat writes a file that will be committed to a public repo |
| build input → static Pages artifact | vite.config.js controls what URL paths the public shell exposes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/Integrity | scripts/heartbeat.mjs writing data/heartbeat.json | mitigate | Atomic same-dir temp+rename (mirror io.mjs writeAtomic) so a crash never commits a half-written file; whole-file commits only (preserves DATA-05) |
| T-04-02 | Information disclosure | heartbeat.json content in public repo | accept | File holds only an ISO timestamp — no PII, no secrets, no marktguru keys |
| T-04-03 | Tampering | per-store lastUpdated falsely refreshed by heartbeat | mitigate | D-05 invariant test asserts stores[].lastUpdated is byte-unchanged; separate file cannot touch stores[] by construction |
| T-04-04 | Spoofing/Integrity | absolute manifest paths breaking install under subpath | mitigate | Keep start_url/scope/icon srcs relative; base-only change; build verified to emit /ColaApp/ asset URLs |
| T-04-SC | Tampering | npm installs (web build) | mitigate | No new packages this plan; `npm ci` is lockfile-exact; package legitimacy audit in RESEARCH shows zero new deps |
</threat_model>

<verification>
- `npm test` (repo root) green including the new heartbeat test and the D-05 invariant
- `node scripts/heartbeat.mjs` produces data/heartbeat.json (valid ISO lastRun)
- `cd web && npm run build` produces a /ColaApp/-subpath dist with manifest + sw.js + the three icons
</verification>

<success_criteria>
The keepalive heartbeat writer and the Vite subpath config both exist, are tested/built, and honor the frozen contract (no schema edit) and the D-05 freshness invariant — ready for the CI workflow in Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/04-live-integration-all-stores-hardening/04-01-SUMMARY.md` when done
</output>
