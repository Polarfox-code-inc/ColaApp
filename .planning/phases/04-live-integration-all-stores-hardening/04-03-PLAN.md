---
phase: 04-live-integration-all-stores-hardening
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - web/README.md
autonomous: false
requirements: [INFR-01, INFR-02, INFR-03]
must_haves:
  truths:
    - "The full loop is proven live: cron/dispatch → scraper → committed data → Pages deploy → the installed PWA reflects the new data (success-criterion 3)"
    - "The app installs to the Android home screen from the live HTTPS Pages URL and shows last-fetched data offline + fresh data when online (PWA-01/02/03)"
    - "All five stores render with fault isolation live; Wasgau shows 'nicht automatisch verfügbar' (success-criterion 4)"
    - "Phase 3's deferred 03-05 human-verify checkpoint is closed (D-12)"
  artifacts:
    - path: "web/README.md"
      provides: "Real-device live verification procedure extending the existing localhost procedure (D-12)"
      contains: "polarfox-code-inc.github.io/ColaApp"
  key_links:
    - from: "live Pages URL https://polarfox-code-inc.github.io/ColaApp/"
      to: "/ColaApp/data/current-offers.json"
      via: "relative fetch resolving against document.baseURI under the subpath (D-07)"
      pattern: "/ColaApp/data/"
---

<objective>
Prove the live loop end-to-end on the real target environment and close out the phase: trigger the pipeline, confirm the scrape→commit→deploy chain actually updates the served data, then run the folded-in Phase-3 03-05 real-device acceptance (install to Android home screen, offline last-data, fresh-when-online, six-state visual check) against the live HTTPS Pages URL. Record the procedure in web/README.md so it is repeatable.

Purpose: Success-criteria 3 (full loop) and 4 (all five stores fault-isolated live) plus the keepalive-loop half of INFR-03 can only be verified against the live Pages deployment on the actual phone (D-12). This is the true acceptance environment; passing it closes Phase 3's open 03-05 checkpoint.
Output: a documented live verification procedure in web/README.md and a human-confirmed pass of the full loop + real-device checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-integration-all-stores-hardening/04-CONTEXT.md
@.planning/phases/04-live-integration-all-stores-hardening/04-RESEARCH.md
@web/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the live full-loop + real-device verification procedure (D-12)</name>
  <files>web/README.md</files>
  <read_first>
    - web/README.md — the existing dev/build/preview + ?state= fixture switch + localhost install/offline procedure (the Phase-3 03-05 artifact this extends)
    - .planning/phases/04-live-integration-all-stores-hardening/04-RESEARCH.md — the "Verification Procedure (D-12)" section (steps 1-8) and the live URL https://polarfox-code-inc.github.io/ColaApp/
    - .planning/phases/04-live-integration-all-stores-hardening/04-CONTEXT.md — D-12 (what the folded-in verification must cover)
  </read_first>
  <action>
    Add a "Live verification (production / D-12)" section to web/README.md that extends the existing localhost procedure to the live HTTPS Pages URL https://polarfox-code-inc.github.io/ColaApp/ on a real Android phone. Transcribe RESEARCH's Verification Procedure steps 1-8 as a numbered checklist: (1) loop proof — trigger via workflow_dispatch or cron, confirm green run → `chore(data): …` commit on master → deploy job ran (gated) → live URL serves the new current-offers.json (footer "last updated"); (2) install via Android Chrome "Add to home screen" — name ColaApp, trademark-safe bottle icon on #1A1D21, maskable not clipped, opens standalone; (3) offline last-data via airplane mode reopen; (4) fresh-when-online after a data-changing scrape (StaleWhileRevalidate revalidates); (5) six ?state= fixtures render per the existing README table; (6) German/de-DE formatting (€9,99, 0,83 €/l, 21.06.2026); (7) subpath sanity — SW registered under /ColaApp/, manifest loaded, icons resolve at /ColaApp/icon-*.png (no 404s); (8) fault isolation — all five stores present, Wasgau "nicht automatisch verfügbar", one store error doesn't blank the others. Also note the one-time maintainer prerequisites (Pages source = GitHub Actions, repo public, token read+write) reference Plan 02. Keep it documentation only — no product-code change.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('web/README.md','utf8');const need=['polarfox-code-inc.github.io/ColaApp','Add to home screen','airplane','?state=','nicht automatisch verfügbar'];const miss=need.filter(n=>!r.includes(n));if(miss.length){console.error('README missing:',miss);process.exit(1)}console.log('README live-verify section OK')"</automated>
  </verify>
  <acceptance_criteria>
    - web/README.md contains a numbered live-verification section referencing the live URL and covering install, offline, fresh-when-online, six states, subpath sanity, and fault isolation
    - The maintainer one-time prerequisites are referenced
    - No product-code files were modified
  </acceptance_criteria>
  <done>web/README.md documents a repeatable live full-loop + real-device verification procedure (D-12) extending the localhost one.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live full-loop + real-device acceptance (closes 03-05, D-12)</name>
  <what-built>
    The complete live pipeline (Plans 01-02) is deployed: pipeline.yml runs the scraper, commits data + heartbeat, and deploys the PWA + data to https://polarfox-code-inc.github.io/ColaApp/. The verification procedure is in web/README.md.
  </what-built>
  <read_first>
    - web/README.md — the "Live verification (production / D-12)" section authored in Task 1 (the exact steps to run)
    - .planning/phases/04-live-integration-all-stores-hardening/04-RESEARCH.md — Verification Procedure steps 1-8 and the Manual Maintainer Checklist (do the one-time GitHub settings first if not already done)
  </read_first>
  <how-to-verify>
    PREREQUISITE (one-time, human-only — RESEARCH Manual Maintainer Checklist): in GitHub repo Settings confirm repo is Public, Pages source = "GitHub Actions", Actions workflow permissions = Read and write, default branch = master, and failed-workflow email is on for knut_ulf@web.de.

    Then run the web/README.md "Live verification" checklist on your Android phone against https://polarfox-code-inc.github.io/ColaApp/:
    1. Trigger the workflow (Actions tab → Run workflow / workflow_dispatch). Confirm: the run is green; a `chore(data): scheduled scrape + heartbeat` commit appears on master; the deploy job ran (gated correctly); the live URL's footer shows an updated "last updated".
    2. Open the live URL in Android Chrome → "Add to home screen". Confirm name ColaApp, the trademark-safe bottle icon on #1A1D21 (not Coca-Cola red), maskable not clipped, opens standalone (no browser chrome).
    3. With the app loaded once, enable airplane mode and reopen — confirm hero/cards/graph/footer render last-fetched data (not an offline error).
    4. Back online, after a data-changing scrape, reopen the app twice — confirm the new price appears (StaleWhileRevalidate).
    5. Visit …/ColaApp/?state=offer|no_offer|upcoming|error|stale|unavailable — confirm each renders per the README table.
    6. Confirm German throughout and de-DE formatting (€9,99, 0,83 €/l, 21.06.2026).
    7. Confirm (DevTools/remote debug) the SW registered under /ColaApp/, manifest loaded, icons resolve at /ColaApp/icon-*.png (no 404s), and data fetched from /ColaApp/data/….
    8. Confirm all five stores present; Wasgau shows "nicht automatisch verfügbar"; a single store error doesn't blank the others.
  </how-to-verify>
  <acceptance_criteria>
    - All 8 live-verification steps pass against the production Pages URL on a real Android device
    - The scrape→commit→deploy→installed-PWA loop is observed to reflect new data (success-criterion 3)
    - INFR-01, INFR-02, INFR-03 are confirmed satisfied; Phase 3's 03-05 checkpoint is marked closed (D-12)
  </acceptance_criteria>
  <resume-signal>Type "approved" when all 8 steps pass, or describe which step failed</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Every file/symbol Phase 4 creates or changes (across all three plans):

| Artifact | Plan | Type | Notes |
|----------|------|------|-------|
| `scripts/heartbeat.mjs` | 04-01 | new file | Zero-dependency keepalive writer; writes `data/heartbeat.json` `{ lastRun: ISO }` atomically; never touches stores[].lastUpdated (D-04/D-05) |
| `data/heartbeat.json` | 04-01/04-02 | new runtime data file | Produced each run by the heartbeat script; committed by the workflow (keepalive activity) |
| `test/heartbeat.test.mjs` | 04-01 | new test | Locks the D-05 invariant (per-store lastUpdated unchanged) + ISO/serialization checks |
| `web/vite.config.js` → `base: '/ColaApp/'` | 04-01 | one-line edit | Subpath correctness for Pages (D-08); the only product-code change |
| `.github/workflows/pipeline.yml` | 04-02 | new file (greenfield) | Single scrape→commit→build→deploy workflow; schedule 2×/day + push + workflow_dispatch (D-01/D-06..D-11) |
| Optional `gh workflow enable` step | 04-02 | conditional | Only if L-1 decision = gh-reenable-step |
| `web/README.md` → live-verify section | 04-03 | doc edit | D-12 real-device procedure extending the localhost one |

Non-code (human, one-time, captured in 04-02 user_setup): repo Public, Pages source = "GitHub Actions", token Read and write, confirm default branch master + failure-email.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public Pages CDN → installed PWA on phone | World-readable shell + data served over HTTPS to the end-user device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information disclosure | data served publicly at /ColaApp/data/ | accept | Data is public supermarket offer prices for one product — no PII, no secrets; intentionally world-readable |
| T-04-11 | Tampering | stale/wrong data served after a missed run | mitigate | OFFR-06 stale chip (D-03) surfaces a long gap to the maintainer; HTTPS (Pages) prevents in-transit tampering |
| T-04-SC | Tampering | npm/Action installs | mitigate | This plan is doc + manual verification only; no installs |
</threat_model>

<verification>
- web/README.md live-verification section present and assertion-checked
- Human checkpoint confirms all 8 live steps pass on the real device
- On pass: INFR-01/02/03 satisfied; Phase 3's 03-05 checkpoint closed (D-12)
</verification>

<success_criteria>
The full free, self-sustaining loop is verified live end-to-end on the real Android device, all five stores render fault-isolated with Wasgau "nicht automatisch verfügbar", and the deferred Phase-3 acceptance is closed — completing Phase 4.
</success_criteria>

<output>
Create `.planning/phases/04-live-integration-all-stores-hardening/04-03-SUMMARY.md` when done
</output>
